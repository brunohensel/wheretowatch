CREATE TABLE IF NOT EXISTS streamProviderEntity(
id INTEGER AS kotlin.Int PRIMARY KEY NOT NULL,
name TEXT NOT NULL,
logoPath TEXT NOT NULL
);

insertProvider:
INSERT OR REPLACE INTO streamProviderEntity(
id,
name,
logoPath
) VALUES (
?,
?,
?
)
;

getProviders:
SELECT * FROM streamProviderEntity
;
